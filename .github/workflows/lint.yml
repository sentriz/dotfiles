name: "lint on push"
on:
  push:
    branch:
      - master
jobs:
  shellcheck:
    name: shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: run shellcheck
        uses: ludeeus/action-shellcheck@master
