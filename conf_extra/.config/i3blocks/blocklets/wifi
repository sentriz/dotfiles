#!/usr/bin/env bash

# requires:
# - networkmanager

ssid="$(nmcli -t -f name,type,state connection | grep -Po '[^:]+(?=:802-11-wireless:activated)')"
test -z "$ssid" && exit

test "$BLOCK_BUTTON" -eq 1 2>/dev/null && notify-send.sh -- "$(ip addr | grep -Po 'inet\s\K.*?\s')"
echo "wifi $ssid"
