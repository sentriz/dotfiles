#!/usr/bin/env bash

out=""

ponymix --sink is-muted \
    && out+="muted" \
    || out+="$(ponymix --sink get-volume || echo "-1")%"

ponymix --input is-muted \
    && out+=", mic muted"

echo "volume $out"
