#!/usr/bin/env bash

# requires:
# - alsa-utils

resp="$(amixer sget "$BLOCK_INSTANCE")"
grep -q '\[off\]' <<< "$resp" && echo 'muted' && exit

echo "volume $(grep -Po 'Left.*\[\K([0-9]+%)(?=\])' <<< "$resp")"
