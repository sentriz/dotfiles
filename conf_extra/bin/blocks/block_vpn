#!/usr/bin/env bash

# requires:
# - networkmanager

function get-profile() {
    nmcli -t -f name,type,state connection \
        | grep -Po '[^:]+(?=:vpn:activated)'
}

profile="$(get-profile)"
test -z "$profile" && exit

echo "vpn $profile"
