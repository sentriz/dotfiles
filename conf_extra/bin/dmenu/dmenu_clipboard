#!/usr/bin/env bash

if ! type -p clipman >/dev/null; then
    echo "please install clipman" >&2
    echo "https://github.com/yory8/clipman/" >&2
    exit 1
fi

clipman pick \
    -t CUSTOM \
    -T "dmenu_custom --prompt clipboard"
