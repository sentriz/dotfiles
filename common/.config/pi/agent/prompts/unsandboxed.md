---
description: Retry the last thing now that the sandbox is gone
---
I've restarted you with `PI_UNSANDBOXED=1`, so the sandbox restrictions that likely broke
the last command are gone. Try it again.
