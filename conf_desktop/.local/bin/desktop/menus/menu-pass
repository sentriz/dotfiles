#!/usr/bin/env bash

if [[ "$#" -gt 0 ]]; then
    IFS=$'\t' read -r id _ <<<"$1"
    rbw get "$id" | wl-copy -n --sensitive
    exit
fi

rbw list --fields id --fields folder --fields name --fields user \
    | sort -t$'\t' -k 2,3
