#!/usr/bin/env bash

if [[ "$#" -gt 0 ]]; then
    IFS=$'\t' read -r id _ <<<"$1"
    case "$id" in
    lock) swaylock ;;
    exit) swaymsg exit ;;
    reload) swaymsg reload ;;
    esac
    exit
fi

printf '%s\t%s\n' \
    lock "the screen" \
    exit "sway" \
    reload "sway config"
