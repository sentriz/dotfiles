#!/usr/bin/env bash

if [[ "$#" -gt 0 ]]; then
    wl-copy -n <<<"$1"
    exit
fi

if [[ -z "$CMENU_INPUT" ]]; then
    echo "$(cmenu label)no input"
    exit
fi

pi --model openai/gpt-5.6-luna -p "$CMENU_INPUT"
exit 0
