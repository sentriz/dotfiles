#!/usr/bin/env bash

set -u

if [[ "$#" -gt 0 ]]; then
    IFS=$'\t' read -r _ _ cmd <<<"$1"
    swaymsg exec -- "$cmd"
    exit
fi

cmenu set \
    colour 2 \
    hide 3

xdg-desktop-list \
    | awk -F'\t' '{ printf "%s\t%s\t%s\n", $1, $2, $3 }'
