#!/usr/bin/env bash

set -u

if [[ "$#" -gt 0 ]]; then
    wl-copy -n <<<"$1"
    exit
fi

cmenu set \
    colour 2 \
    debounce 100ms

if [[ -z "${CMENU_INPUT-}" ]]; then
    echo "$(cmenu label)no input"
    exit
fi

numb "${CMENU_INPUT-}" || true
