#!/usr/bin/env bash

dir="$(mktemp -d "$DOTS_SCRAP_DIR/go.XXX")"
cd "$dir" || exit 1

main="main.go"
main_abs="$dir/$main"

go mod init "${dir##*\.}"
cat > "$main" << EOL
// $ go run "$main_abs"

package main

func main() {}
EOL

nvim "$main"
