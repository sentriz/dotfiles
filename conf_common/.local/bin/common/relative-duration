#!/usr/bin/env fish

set -l secs $argv[1]

if test "$secs" -lt 60
    echo "$secs"s
else if test "$secs" -lt 3600
    echo (math -s0 "$secs / 60")m
else
    echo (math -s0 "$secs / 3600")h (math -s0 "($secs % 3600) / 60")m
end
