#!/bin/bash

echo locking
shot=$(mktemp --tmpdir XXXX.png)
scrot $shot
convert $shot -blur 0x3 -gravity north -fill "#ababab" -pointsize 40 -annotate +0+90 "locked at $(date +%H:%m)" $shot
convert $shot ~/bin/lock.png -gravity center -geometry +0+0 -composite $shot
i3lock -u -i $shot
rm $shot
