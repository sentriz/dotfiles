#!/usr/bin/bash


if grep -qs "10.0.0.1" /tmp/osmc_host && grep -qs "$HOME/dynamic" /proc/mounts; then
    mpd --kill
    fusermount -qu $HOME/dynamic 
    nmcli device disconnect enp0s25 &> /dev/null
else
    notify-send "didn't unmount dynamic"
fi

projector --single
