#!/bin/bash

if [ "$(id -u)" != "0" ]; then
    exec sudo "$0" "$@" 
fi

ifconfig wlp3s0 up
nmcli connection up "home wifi"
nmcli connection down "home wired"
ifconfig enp0s25 down

projector --single
/home/senan/.fehbg
killall -SIGUSR1 autossh

rm /tmp/docked
