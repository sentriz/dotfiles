[Unit]
Description=Delete jellyfin transcodes

[Service]
Type=oneshot
ExecStart=/bin/sh -c ' \
	rm --one-file-system -rfv /opt/containers/jellyfin/config/transcoding-temp/* 2>&1 \
	| mail -s "status for clean-jellyfin.service" -- senan@senan.xyz \
'
